name: using-command-with-workflow
on:
  workflow_dispatch:
jobs:
  gob-1:
    runs-on: ubuntu-latest
    steps:
      - name: job1
        run: |
          depercated=true
          if [[ depercated ]]; then
            echo "::warning::Deprecated found"
          fi
  
