name: ci-the-app
on:
  workflow_dispatch:

jobs:
  job-ci:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-the-repo
        uses: actions/checkout@v6-beta
           
      - name: test-the-app
        run: npm test
